<?xml version="1.0" encoding="UTF-8"?>

<beans:beans
    xmlns="http://www.springframework.org/schema/security"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:beans="http://www.springframework.org/schema/beans"    
    xsi:schemaLocation="http://www.springframework.org/schema/beans
        			 http://www.springframework.org/schema/beans/spring-beans.xsd
        			 http://www.springframework.org/schema/security
        			 http://www.springframework.org/schema/security/spring-security.xsd">
        						
<beans:bean name="mvcHandlerMappingIntrospector" class="org.springframework.web.servlet.handler.HandlerMappingIntrospector" />

<http use-expressions="true">

	<!--  정적 리소스 허용  -->
	<intercept-url pattern="/resources/**" access="permitAll"/>
	<intercept-url pattern="/jomorning_upload/**" access="permitAll"/>
	
	<!--  로그인 허용  -->
	<intercept-url pattern="/login" access="permitAll"/>
	<intercept-url pattern="/login/**" access="permitAll"/>
	
	<!--  관리자 전용 (PUT, DELETE 요청은 관리자 권한만 가능)  -->
	<intercept-url pattern="/users/**" method="PUT" access="hasRole('ADMIN')"/>
	<intercept-url pattern="/users/**" method="DELETE" access="hasRole('ADMIN')"/>
	
	<!--  로그인한 사용자  -->
	<intercept-url pattern="/users/**" access="isAuthenticated()"/>
	
	<!--  그 외는 허용  -->
	<intercept-url pattern="/**" access="permitAll"/>
	
	<form-login login-page="/login"
				login-processing-url="/login"
				default-target-url="/users"
				username-parameter="username"
				password-parameter="password"
				authentication-failure-url="/login?error=true"/>
				
	<!-- 로그아웃 처리할 요청 (= 로그아웃 버튼 있는 <form> 태그 action 속성값 일치 (POST 방식) -->
	<!-- (기본값) 로그아웃 성공시 이동할 경로 지정 (GET 방식) -->			
	<logout logout-url="/logout" logout-success-url="/login?logout=true"
					invalidate-session="true" delete-cookies="JSESSIONID"/>
	<!-- CSRF 사용 -->					
	<csrf disabled="true"/>
</http>

<authentication-manager>
	<authentication-provider>
		<password-encoder ref="passwordEncoder"/> <!-- {noop} 대체 -->
			<user-service>
				<user name="admin" password="admin" authorities="ROLE_ADMIN, ROLE_USER"/>
				<user name="guest" password="guest" authorities="ROLE_USER"/>
			</user-service>
		</authentication-provider>
	
	<!-- JDBC - DB 계정 정보 받아오는 provider -->
		<authentication-provider>
			<password-encoder ref="passwordEncoder"/> <!-- {noop} 대체 -->
			<jdbc-user-service data-source-ref="dataSource"
							   users-by-username-query="SELECT u_id, u_pw, 1 FROM user WHERE u_id = ?"
							   authorities-by-username-query="SELECT u_id, u_role FROM user WHERE u_id = ?"/>
		</authentication-provider>
	</authentication-manager>
</beans:beans>